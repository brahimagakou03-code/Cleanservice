node_modules

# Fichiers temporaires locaux (tests curl / scripts)
.tmp/
# Keep environment variables out of version control
.env
.env.*
!.env.example

# SQLite local
*.db
*.db-journal
prisma/dev.db*

# Fichiers uploadés (garder le dossier vide dans Git)
public/uploads/*
!public/uploads/.gitkeep

/generated/prisma
